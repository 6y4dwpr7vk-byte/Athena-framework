{
  "name": "athena-system",
  "version": "1.0.0",
  "description": "Athena — Boundary-Respecting Systems. Framework for institutional analysis and judgment boundary mapping.",
  "scripts": {
    "start": "concurrently \"npm run start:app\" \"npm run start:worker\"",
    "start:app": "cd app && python3 -m http.server 3000",
    "start:worker": "cd worker && npx wrangler dev",
    "dev": "npm run start",
    "worker": "cd worker && npx wrangler dev",
    "app": "cd app && python3 -m http.server 3000",
    "deploy:worker": "cd worker && npx wrangler deploy"
  },
  "keywords": [
    "athena",
    "boundary-analysis",
    "institutional-framework",
    "judgment-boundary",
    "diagnostic-tool"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/6y4dwpr7vk-byte/Athena-framework.git"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
